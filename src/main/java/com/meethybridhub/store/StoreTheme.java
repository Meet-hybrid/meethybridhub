package com.meethybridhub.store;

/**
 * Storefront theme choices for store branding (Phase 3, Week 4).
 * Stored as the enum name in a VARCHAR(20) column — appending values is safe,
 * renaming existing values is NOT.
 */
public enum StoreTheme {
    LIGHT,
    DARK
}
